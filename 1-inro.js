//  globals - no window

// _dirname - path to current directory
// _filename - file name
// require - function to use modules (coomonjs)
// module - info about current module(file)
// process - info about env where the prohram is being executed

console.log(__dirname)
setInterval(() => {
    console.log('hello world')
}, 1000)